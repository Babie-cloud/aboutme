/** Static site config */
export const GITHUB_USERNAME = 'Babie-cloud';

/** Your résumé in `/public/` (exact filename). */
export const CV_PDF_URL = '/Karlie_cubahiro.pdf';

/**
 * Pinned repositories: slug list in profile order (top pin first), e.g. `['aboutme', 'portfolio']`.
 * Only **your** repos load (`GET /repos/{user}/{slug}`); org contributions are excluded unless you pin your fork.
 */
export const GITHUB_PINNED_REPO_SLUGS = [];
